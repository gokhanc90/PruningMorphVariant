# Versions this work was run with. Later versions are expected to work; these
# are recorded so that a run can be reproduced exactly.
numpy==2.5.1
pandas==3.0.5
scipy==1.18.0
pyarrow==25.0.0
openpyxl==3.1.5

# scoring
torch==2.13.0
transformers==5.14.1
sentence-transformers==5.6.1
gensim==4.4.0

# evaluation (MAP; the trec_eval bindings)
pytrec_eval-terrier==0.5.10
